onboarding = false

[theme]
# No built-in "tairiki"; use a neutral dark base and override every token below.
name = "one-dark"

# tairiki.nvim dark palette (deparr/tairiki.nvim, lua/tairiki/palette/dark.lua)
[theme.custom]
panel_bg      = "#151515"  # bg
sidebar_bg    = "#151515"  # bg
active_row_bg = "#282828"  # bg_light2
selection_bg  = "#373b41"  # ghostty selection-background
surface0      = "#1d1f21"  # bg_light
surface1      = "#282828"  # bg_light2
surface_dim   = "#101010"  # below bg
overlay0      = "#3b3f4c"  # bg_light3
overlay1      = "#696969"  # fg_dark3
text          = "#c5c8c6"  # fg
subtext0      = "#969896"  # fg_dark2
accent        = "#de935f"  # orange
red           = "#cc6666"
green         = "#b5bd68"
yellow        = "#f0c674"
blue          = "#81a2be"
teal          = "#7eada7"  # cyan
peach         = "#de935f"  # orange
mauve         = "#b294bb"  # purple

[ui]
show_agent_labels_on_pane_borders = true
accent = "#de935f"
# tmux drew single-line dividers between adjacent panes
pane_gaps = false

# kill-window and kill-session never asked
confirm_close = false
# automatic-rename gave windows a name without prompting
prompt_new_tab_name = false
# set -g mouse on
mouse_capture = true

agent_panel_sort = "spaces"
[ui.toast]
delivery = "herdr"

[terminal]
# Matches -c "#{pane_current_path}" on every split, window, and session
new_cwd = "follow"

[keys]
prefix = "ctrl+b"

# Config and help
reload_config = "prefix+q"
help = "prefix+?"
detach = "prefix+d"
# Copy mode
copy_mode = "prefix+["

# Panes
split_horizontal = ["prefix+h", "alt+enter"]
split_vertical = ["prefix+v", "alt+shift+enter"]
close_pane = ["prefix+x", "alt+esc"]
zoom = "prefix+z"
last_pane = "prefix+;"
focus_pane_left = "ctrl+alt+left"
focus_pane_down = "ctrl+alt+down"
focus_pane_up = "ctrl+alt+up"
focus_pane_right = "ctrl+alt+right"
resize_mode = ["prefix+ctrl+left", "prefix+ctrl+down", "prefix+ctrl+up", "prefix+ctrl+right"]

# No tmux equivalent; herdr's default prefix+shift+p is taken by previous session
rename_pane = "prefix+shift+o"
# Windows -> tabs
new_tab = "prefix+c"
rename_tab = "prefix+r"
close_tab = "prefix+k"
switch_tab = ["prefix+1..9", "alt+1..9"]
previous_tab = ["prefix+p", "alt+left"]
next_tab = ["prefix+n", "alt+right"]

# Sessions -> workspaces
rename_workspace = "prefix+shift+r"
close_workspace = "prefix+shift+k"
previous_workspace = ["prefix+shift+p", "alt+up"]
next_workspace = ["prefix+shift+n", "alt+down"]

# Resize by ~5 columns/rows, like resize-pane -L 5 on C-M-S-arrows
[[keys.command]]
key = "ctrl+alt+shift+left"
type = "shell"
command = "herdr pane resize --current --direction left --amount 0.03"

[[keys.command]]
key = "ctrl+alt+shift+down"
type = "shell"
command = "herdr pane resize --current --direction down --amount 0.03"

[[keys.command]]
key = "ctrl+alt+shift+up"
type = "shell"
command = "herdr pane resize --current --direction up --amount 0.03"

[[keys.command]]
key = "ctrl+alt+shift+right"
type = "shell"
command = "herdr pane resize --current --direction right --amount 0.03"

